swagger: '2.0'
produces: [application/json]
schemes: [http]
basePath: "/api"
consumes: [application/json]
info:
  title: API for testing path matching
  version: "1.0.0"
paths:
  /path:
  /path.json:
  /get/{id}:
    parameters:
      - name: id
        in: path
        type: integer
        required: True
    responses:
  /post:
